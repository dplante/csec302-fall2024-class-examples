package dev.csec302;

public class Book {
    public Book(String title, String author, int numPages) {
        // Constructor
    }   

    public boolean flipPage(boolean forward) {
        // Flips the page forward or backward
        return true;
    }

    public boolean bookmarkPage() {
        // Bookmarks the current page
        return true;
    }

    public boolean openBook() {
        // Opens the book
        return true;
    }

    public boolean closeBook() {
        // Closes the book
        return true;
    }

    public boolean openToBookmark() {
        // Opens the book to the bookmarked page
        return true;
    }

    public boolean turnToPage(int pageNumber) {
        // Turns to a specific page
        return true;
    }

    public boolean isOpen() {
        // Returns true if the book is open
        return true;
    }

    public boolean isClosed() {
        // Returns true if the book is closed
        return true;
    }

    // Setters and getters
    public int getPageNumber() {
        return 0;
    }
}
